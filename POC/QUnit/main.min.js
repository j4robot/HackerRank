export function GenerateUnitTest(mod, msg, method, params, answer){
    QUnit.module(mod, () => {
        QUnit.test(msg, function(assert){
            assert.equal(params ? method(...params) : method(), answer);
        })
    });
}